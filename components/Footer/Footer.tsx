import React from "react";
import Section from "components/Section";
import Box from "@mui/material/Box";
import { gridP } from "theme";

const Footer = () => {
  return (
    <Section>
      <Box p={gridP}>Footer</Box>
    </Section>
  );
};

export default Footer;
